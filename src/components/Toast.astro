<div class="toast-notification" id="toast">
  <div class="toast-content">Thanks! Snoodev will get back to you shortly.</div>
</div>

<style>
  .toast-notification {
    visibility: hidden;
    width: 100%;
    height: 12em;
    display: flex;
    align-items: center;
    justify-content: center;
    position: fixed;
    left: 50%;
    bottom: 2em;
    transform: translateX(-50%);
    background-color: var(--color-accent);
    border-radius: var(--border-radius);
    border: 2px dashed var(--color-text);
  }

  .toast-content {
    font-size: large;
    font-weight: bold;
    padding: 5%;
  }

  @media only screen and (max-width: 900px) {
    .toast-notification {
      width: 100%;
      height: 25em;
    }

    .toast-content {
      font-size: 2em;
      font-weight: bold;
      padding: 5%;
    }
  }
</style>
