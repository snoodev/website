{
  "name": "@example/basics",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "format": "npx prettier --write .",
    "dev": "npx prettier --write . && astro dev",
    "start": "npx prettier --write . && astro dev",
    "build": "npx prettier --write . && astro build",
    "preview": "npx prettier --write . && astro preview",
    "astro": "npx prettier --write . && astro"
  },
  "dependencies": {
    "astro": "^1.6.10",
    "firebase": "^9.14.0"
  },
  "devDependencies": {
    "eslint": "^8.28.0",
    "eslint-plugin-astro": "^0.21.0",
    "prettier": "^2.8.0",
    "prettier-plugin-astro": "^0.7.0"
  }
}
